@LIFE_LIST


REPEAT CHARANUM
	IF CFLAG:COUNT:1 == 0 && COUNT != 0 && BASE:COUNT:0 > 0
		PRINTFORM [{COUNT}] %SAVESTR:COUNT% {CFLAG:COUNT:9}LV
		SIF CFLAG:COUNT:1 == 0 &&CFLAG:COUNT:0 > 0 && COUNT != 0 && BASE:COUNT:0 > 0
			PRINT [매각가능]
		SIF CFLAG:COUNT:1 == 0 &&CFLAG:COUNT:0 == 2 && COUNT != 0 && BASE:COUNT:0 > 0
			PRINT [조수가능]
		;애정, 음란
		IF TALENT:COUNT:애정
			SETCOLOR 255,100,100
			PRINT <애정>
			RESETCOLOR
		ELSEIF TALENT:COUNT:음란
			SETCOLOR 255,100,100
			PRINT <음란>
			RESETCOLOR
		ELSE
			SETCOLOR 100,100,100
			PRINT <미함락>
			RESETCOLOR
		ENDIF
		IF CFLAG:COUNT:1 == 12
			SETCOLOR 100,200,100
			PRINT [파견]
			RESETCOLOR
		ENDIF
		PRINTL  
	ENDIF
REND
DRAWLINE
RETURN 0

;----------------------------
@LIFE_LIST_ENEMY
#DIM LIFE_COUNT
;----------------------------
;逆に, 勇者だけの選択肢です

FOR LIFE_COUNT, 0, CHARANUM
	IF CFLAG:LIFE_COUNT:1 == 2 && LIFE_COUNT != 0 && BASE:LIFE_COUNT:0 > 0
		PRINTFORM [{LIFE_COUNT}] %SAVESTR:LIFE_COUNT% {CFLAG:LIFE_COUNT:9}LV
		SIF CFLAG:LIFE_COUNT:1 == 0 &&CFLAG:LIFE_COUNT:0 > 0 && LIFE_COUNT != 0 && BASE:LIFE_COUNT:0 > 0
			PRINT [매각 가능]
		SIF CFLAG:LIFE_COUNT:1 == 0 &&CFLAG:LIFE_COUNT:0 == 2 && COUNT != 0 && BASE:LIFE_COUNT:0 > 0
			PRINT [조수 가능]
		;종족・性格
		
		PRINTFORM  [%GET_LOOK_INFO(LIFE_COUNT, "종족")%
		PRINTFORM  - %GET_LOOK_INFO(LIFE_COUNT, "성격")%]
		
		;애정, 음란
		IF TALENT:LIFE_COUNT:애정
			SETCOLOR 255,100,100
			PRINT <애정>
			RESETCOLOR
		ELSEIF TALENT:LIFE_COUNT:음란
			SETCOLOR 255,100,100
			PRINT <음란>
			RESETCOLOR
		ELSE
			SETCOLOR 100,100,100
			PRINT <미함락>
			RESETCOLOR
		ENDIF
		PRINTL  
	ENDIF
NEXT
DRAWLINE
RETURN 0

;-------------------------------
@SELECT_YES_NO
;-------------------------------
;はいかいいえを選択するだけの関数です

$INPUT_LOOP

PRINTL   [0] 네   [1] 아니오

INPUT

IF RESULT == 0 || RESULT == 1
	RETURN RESULT
ELSE
	GOTO INPUT_LOOP
ENDIF



